#pragma once
#ifndef HARPY_WINDOW
#define HARPY_WINDOW
#include <utilities/harpy_little_error.h>

namespace harpy::nest::windowing
{
    
    class base_window
    {

    /*
     * Plan for window:
     * 1) Move everything from base_window_layout
     * 2) Resize, name/rename title, do_close, icon
     * 3) Getters/setters
     * 4) fullscreen, v-sync
     * 5) Poll_events
     * 6) lock/unlock cursor, make it visible/not
     * 7) Aspect ratios
     */
        
    public:

    
    
};
}

#endif //HARPY_WINDOW